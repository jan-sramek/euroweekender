apiVersion: v1
kind: Service
metadata:
  name: weekend-api-service
spec:
  type: NodePort
  selector:
    app: weekend-api
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30080